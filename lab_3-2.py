int(a)
#a is not defined
int('a')
#'a' is not an integer, and it is a string, not a variable
'a'+2
#a is not defined. Additionally, a is a string, not a variable
import date
#There is nothing to import named "date"
print("I'm a happy camper)
#Missing a "" at the end which closes the string
