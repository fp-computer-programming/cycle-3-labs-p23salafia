from random import randint, random, seed
import random
from random import randint, randrange

print(randint(1,100)) #The numbers are different every time

print(seed(5))
print(randint(1,100)) #The number comes out the same every time
